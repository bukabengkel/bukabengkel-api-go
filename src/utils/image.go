package utils

// func BuildUrl(path string, transform map[string]int) string {
// 	if os.Getenv("IMAGEKIT") == "true" {
// 		if transform != nil {
// 			return fmt.Sprintf("%s/%s?tr=w-%d,h-%d", os.Getenv("IMAGEKIT_BASE_URL"), path, transform["width"], transform["height"])
// 		}
// 		return fmt.Sprintf("%s/%s", os.Getenv("IMAGEKIT_BASE_URL"), path)
// 	}
// 	return fmt.Sprintf("%s/%s", FileService.baseUrl, path)
// }
